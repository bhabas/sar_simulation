Create figure
    Nominal space on left
    Sensory space on right

    Create trajectory line from (vel, theta) for both plots that updates automatically
    Create scatterpoint from (vel,theta,d) for both plots that update automatically

    3 sliders for scatter

    Create NN policy classification in nominal space and convert to sensory space